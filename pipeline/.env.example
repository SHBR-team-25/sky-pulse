# Ночной интеграционный retention-тест. Перед запуском выполните:
#   set -a
#   source .env
#   set +a

# YTsaurus и отдельный префикс теста.
YT_PROXY=https://http-proxy-hackathon.demo.ytsaurus.tech/
YT_TOKEN=
YT_USER=
YT_POOL=default
YT_BASE_PATH=//home/hackathon/team25/pipeline_retention_test

# Служебные пути SPYT.
YT_POSITIONS_RAW_CONSUMER_PATH=${YT_BASE_PATH}/positions_raw_consumer
YT_CHECKPOINT_PATH=${YT_BASE_PATH}/spark/checkpoints
YT_CODE_PATH=${YT_BASE_PATH}/spark/code
YT_DISCOVERY_PATH=${YT_BASE_PATH}/spark/discovery

# Пути таблиц. Обычно достаточно изменить только YT_BASE_PATH.
YT_POSITIONS_RAW_PATH=${YT_BASE_PATH}/positions_raw
YT_REF_AIRCRAFT_PATH=${YT_BASE_PATH}/ref_aircraft
YT_REF_AIRPORTS_PATH=${YT_BASE_PATH}/ref_airports
YT_POSITIONS_CURRENT_PATH=${YT_BASE_PATH}/positions_current
YT_POSITIONS_HISTORY_PATH=${YT_BASE_PATH}/positions_history
YT_FLIGHTS_OPEN_PATH=${YT_BASE_PATH}/flights_open
YT_FLIGHTS_SEGMENTS_PATH=${YT_BASE_PATH}/flights_segments
YT_AIRPORT_EVENTS_PATH=${YT_BASE_PATH}/airport_events
YT_DASHBOARD_TOTALS_PATH=${YT_BASE_PATH}/dashboard_totals
YT_DASHBOARD_TREND_PATH=${YT_BASE_PATH}/dashboard_trend
YT_DASHBOARD_TOP_AIRPORTS_PATH=${YT_BASE_PATH}/dashboard_top_airports
YT_DASHBOARD_ROUTES_PATH=${YT_BASE_PATH}/dashboard_routes
YT_DASHBOARD_MANUFACTURERS_PATH=${YT_BASE_PATH}/dashboard_manufacturers
YT_PIPELINE_JOB_STATE_PATH=${YT_BASE_PATH}/pipeline_job_state

# Данные хранятся двое суток: ночной прогон и следующий день на анализ.
# Retention обязан быть больше DASHBOARD_WINDOW_SECONDS.
DATA_RETENTION_SECONDS=172800
# Уже обработанные строки queue сохраняются ещё 6 часов для диагностики.
QUEUE_RETAINED_LIFETIME_SECONDS=21600

# OpenSky. Bbox 10 x 20 градусов стоит 3 credits за запрос.
# Шаг 70 секунд расходует около 3703 из 4000 credits за 24 часа.
OPENSKY_CLIENT_ID=
OPENSKY_CLIENT_SECRET=
OPENSKY_POLL_INTERVAL_SECONDS=70
OPENSKY_BBOX_LAMIN=45.0
OPENSKY_BBOX_LOMIN=5.0
OPENSKY_BBOX_LAMAX=55.0
OPENSKY_BBOX_LOMAX=25.0

# Ресурсы streaming job. Trigger сопоставим с периодом появления новых snapshots.
STREAMING_DRIVER_MEMORY=2g
STREAMING_DRIVER_MEMORY_OVERHEAD=1g
STREAMING_EXECUTOR_MEMORY=4g
STREAMING_EXECUTOR_CORES=2
STREAMING_NUM_EXECUTORS=2
STREAMING_SHUFFLE_PARTITIONS=8
STREAMING_TRIGGER_SECONDS=60
STREAMING_MAX_ROWS_PER_PARTITION=50000

# Segment job: запуск раз в 15 минут. Интервалы увеличены под snapshots раз в 70 секунд.
SEGMENT_INTERVAL_SECONDS=900
AIRPORT_RADIUS_KM=15
FLIGHT_TIMEOUT_SECONDS=3600
MAX_TRANSITION_GAP_SECONDS=600
GROUND_GLITCH_MAX_SECONDS=180
ALLOWED_LATENESS_SECONDS=300
BBOX_EXIT_MARGIN_KM=25

# Aggregate job: пересчёт раз в 10 минут, окно показателей — последние сутки.
DASHBOARD_WINDOW_SECONDS=86400
POSITION_FRESHNESS_SECONDS=900
AGGREGATE_INTERVAL_SECONDS=600

# Используются только для локального тестового HTTP-сервера или моков.
# OPENSKY_TOKEN_URL=https://auth.opensky-network.org/auth/realms/opensky-network/protocol/openid-connect/token
# OPENSKY_STATES_URL=https://opensky-network.org/api/states/all

# Параметры отдельного SPYT-кластера; текущие direct-submit launcher'ы их не меняют.
WORKER_CORES=8
WORKER_NUM=3
WORKER_MEMORY=32G
